
✅ **알아두면 시간 단축**
> Integer.parseInt(str, 3);  
> 3진수인 s를 10진수 

> for (char[] row : charMap) {  
>   Arrays.fill(row, ',); // row 배열의 모든 원소를 '.' 으로 채우라는 의미이다.  
> }

> Arrays.sort(), Collections.sort() ➡️ 정렬 조건 제시 가능  

> new ArrayList<>(graph) ➡️ 독립된 복사본 만들 수 있다.  

> Trie 자료구조  
> - 문자열 탐색에 유리하다.  
> - 루트노드는 항상 비어있다.  
> class Node {  
>   HashMap<Character, Node> child;
> }  

> 문자열에서 특정 문자개수 세는 방법은 다음과 같다.  
> int pCount = s.length() - s.replace("p", "").length();

> - Character.isAlphabetic(c)  
> - ('Z' - 'A' + 1) // 26
> - int offset = Character.isUpperCase(c) ? 'A' : 'a';

✅ **주의** 
> 무지성 int start = 0; int end = 0; 주의해야한다.  
> 해당 값이 터무니없을 수 있다. 

> 플로이드워샬에서 k가 가장 바깥 반복문이다. 

> int 입력조차도 long으로 변환해서 다시 넣어주자. 

> BFS는 한번 방문하면, 안한다. 

> 순서대로 보고 싶은 배열은 꼭 **정렬**을 직접 하자. 

> 큰 수를 만들려면, 무조건 앞에만 크면 된다. 

✅ **사고의 흐름**
소수찾기
> 붙여서 만들 수 있는 모든 수  
> ⬇️  
> 주어진 숫자들로 만들 수 있는 모든 순열
> ⬇️  
> 길이가 정해지지 않은 순열
> ⬇️  
> DFS 트리구조

정수삼각형
> 높이가 500이면, 완전탐색시 2^500 시간복잡도이다. 
> ⬇️  
> 불가능  
> ⬇️  
> 중복 계산이 있네?  
> ⬇️  
> 부분 문제의 반복이네?  
> ⬇️  
> DP/메모이제이션

주식가격 
> 2중 반복문 = 100000 * 100000  
> ⬇️  
> 계속 오르다가 떨어지면 기록하면 된다.  
> ⬇️  
> 오르는 과정을 기록하고 "마지막 요소"와의 비교만 필요하기 때문에, Stack을 사용한다. 

단속카메라
> 가장 끝 점에 카메라를 항상 설치하는게 유리하다.  
> ⬇️  
> 그리디 사고 그 자체이다. 
> ⬇️  
> 그리고, 이 카메라가 커버하지 못하는 구간은 추가로 또 설치하면 된다.  
> ‼️ 주의) 정렬은 필수이다. 

호텔방배정
> k는 10^12, room_number는 200000이므로, 배열 완전탐색은 불가능하다.  
> ⬇️  
> 어떤 방이 차면, 다음에 가능한 방이 알고 싶다.  
> ⬇️  
> 1번 방이 차면 2번 방부터 안내 / 5번 방이 차면 6번 방부터 안내하면 된다. 
> ⬇️  
> 방이 차면, 다음 방으로 연결하자. 
> ‼️ 10^12 길이의 배열을 만들 수 없으니 **HashMap**

✅ **전략/공략**  
1. StringBuilder ~~StringBuffer~~
StringBuffer는 thread-safe하기 때문에 느리다.  
코딩테스트는 대부분 단일 스레드 환경이므로, 빠르다.  
2. 삽입, 삭제가 많은 경우에는 LinkedList가 유리하다. 

